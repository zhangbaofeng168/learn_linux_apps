守护进程：
	不占用终端
	独立于控制终端 setsid();
	周期性运行
	例如：smbd
如何创建守护进程：
	创建子进程，让子进程充当守护线程，父进程退出

拜托父进程的影响：	
chdir("/")改变程序的工作目录
修改文件权限掩码：
	通常的使用方法函数为umask(0)
关闭文件描述符：
	for(i = 0;i < 65535;i++)
		close(i);
	

	